name: Container
on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container: node:16.13.1-alpine3.12
    steps:
      - name: log node version
        run: |
          node -v
          cat /etc/os-release
